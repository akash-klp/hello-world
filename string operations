#include <bits/stdc++.h>

using namespace std;

int main()
{
string val="ajjdh/hfg";

for(int i=0;val[i]!='\0';i++)
{
    cout<<val[i]<<endl;
    if(val[i]=='/')
    {
    cout<<"yes bro it contains /";
    exit(0);
    }
}
cout<<"dosn't cotains /";
    return 0;
}

OUTPUT:

ajjdh/hfg
yes bro it contains /

-------------------------------------------------------------------------------------------------------------
